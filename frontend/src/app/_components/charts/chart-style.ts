// Shared recharts styling constants per wiki/pages/design-system.md's
// "Dashboard adaptation rules": grid stroke rgba(255,255,255,0.06) (=
// --grid-line), axis ticks --fg-muted in --font-data.
export const GRID_STROKE = "var(--grid-line)";

export const AXIS_TICK = {
    fontSize: 10,
    fill: "var(--fg-muted)",
    fontFamily: "var(--font-data)",
};
